# DB connector (Temporary env)
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.password=billionaire
spring.datasource.username=investor
spring.datasource.url=jdbc:mysql://localhost:3306/investment
spring.jpa.database-platform=org.hibernate.dialect.MySQL5Dialect
spring.jpa.hibernate.ddl-auto=update

# Mapping paths
paths.brokeraccounts.management-controller=/accounts/management
paths.brokeraccounts.create-endpoint=/create
paths.brokeraccounts.delete-endpoint=/delete
paths.brokeraccounts.update-endpoint=/update
paths.brokeraccounts.browse-controller=/accounts/browse

# Temporary for development
debug=true
security.basic.enable=false
security.ignored=/**